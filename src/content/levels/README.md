# 关卡内容

手编关卡 JSON（`version` / `id` / `title` / `kind` / `positions` / `edges`）放在本目录。

制作流程（C0）：

1. 自由模式里摆好造型（或随机一局后拖成图形）
2. 菜单 →「导出关卡」复制 JSON
3. 改 `id` / `title` / `kind` 后存为 `*.json`

`kind`：`mainline` | `skin_demo` | `skin`
