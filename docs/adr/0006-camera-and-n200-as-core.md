# 大画布与相机为一等能力，顶点数硬顶 200

差异化暂不做，但主玩法包含大顶点数；通过大于屏幕的画布避免「点挤在一起选不中」。决定：基础完成标准包含**平移、缩放、重置视图**，以及 **n≤200 时仍可玩**的性能预期；手势模型统一（拖点 / 平移 / 双指缩放），不按 n 拆两套。执行上一人开发，内部拆 A0（≤15+相机）→ A1（种子）→ A2（冲 200），整包基础完成后再做包装，再上 Toy。
