# Untangle

B 站 Toy 向的解缠（Planarity）益智游戏：玩家移动顶点，使边不再交叉。本文件只固定领域用语，不描述实现。

## Language

### 谜题结构

**局（Deal）**：
一次可玩的解缠实例，由顶点数、边集与顶点初始位置共同确定。
_Avoid_: 关卡（除非日后做手编关卡表）、游戏、puzzle instance

**顶点数（Vertex Count）**：
一局中顶点的个数，由玩家设定；硬顶为 200。
_Avoid_: 点数（口语可说，文档与代码用语用顶点数）、n（可作变量名，不作领域对外名）

**平面图保证（Planarity Guarantee）**：
每一局的抽象图都是平面图，因此一定存在无交叉画法；卡关是未找到布局，不是无解。
_Avoid_: 随机连边、可能无解模式（当前产品不做）

**圆上布局（Circle Layout）**：
v1 中顶点的初始位置：均匀（或按约定）落在圆上；位置尚不进入种子。
_Avoid_: 随机散点（后置）、固定关卡坐标表（非 v1 主路径）

**交叉（Crossing）**：
两条边在非共享顶点处相交；全无交叉即通关。
_Avoid_: 碰撞、重叠（除非特指顶点重合）

### 复现与分享

**种子（Seed）**：
可复制、可粘贴的短码，用于复现同一局。v1 编码顶点数与连接关系（含生成用随机源），不含顶点坐标。
_Avoid_: 存档文件、关卡 ID（参照站用语，非本产品主名）

**随机一局（Reroll）**：
在当前顶点数下重新生成连接关系，并得到新种子。
_Avoid_: 下一关（若未来做线性关卡表再启用）

### 操作与视图

**拖点（Vertex Drag）**：
选中顶点并移动以改变嵌入，目标是消除交叉。
_Avoid_: 画线、编辑边

**相机（Camera）**：
视口的平移与缩放；画布可大于屏幕，使大顶点数下仍可点选与操作。
_Avoid_: 滚动页面（应用内手势，不是浏览器滚动画布的替代说法）

**重置视图（Reset View）**：
把相机调回使当前全部顶点落入视野的状态。
_Avoid_: 重启本局、重置布局

### 演进（有名、未做）

**主题（Theme）**：
可替换的视觉包装（颜色、材质等），不改变解缠规则。v1 包装落地为一版定妆 + 可换肤数据结构，不做多主题切换 UI。
_Avoid_: 皮肤商城、多主题切换器（非 v1）

**玩法变体（Variant）**：
在解缠内核之上的规则加减（限时、最少移动等）；基础可玩且包装完成后再考虑。
_Avoid_: 模式（易与难度档混淆）

### 分发

**试玩链（Playtest Link）**：
Toy 上架前，将静态构建产物托管在 Pages 类服务上供他人游玩的链接。
_Avoid_: 正式发布、上架（专指 Toy 审核通过后）

**Toy**：
哔哩哔哩站内互动页托管；本产品的目标上架渠道，交付物为静态网页包。
_Avoid_: 小游戏开放平台（另一套 B 站能力）、App 商店
