# Untangle

B 站 Toy 向的解缠（Planarity）益智游戏：玩家移动顶点，使边不再交叉。本文件只固定领域用语，不描述实现。

## Language

### 谜题结构

**局（Deal）**：
一次可玩的解缠实例，由顶点数、边集与顶点初始位置共同确定。
_Avoid_: 游戏、puzzle instance

**关卡（Level）**：
手编的一局：含边集与**图形排布的初始坐标**（不再用均匀圆）。通关只判无交叉，不要求点回到原造型。
_Avoid_: 把关卡说成种子（种子仍不含坐标）

**主线关（Mainline Level）**：
战役主干上的造型关，不绑定 IP 人物。
_Avoid_: 皮肤关、自由局

**皮肤包关（Skin Pack Level）**：
绑定主题/人物的关卡；独立小战役，作引流，难且少更。
_Avoid_: 塞进主线编号的「第 N 关皮肤」

**自由模式（Free Mode）**：
设顶点数、随机一局、种子复现（圆上布局）；主线通关前锁定。
_Avoid_: 自定义关卡编辑器（后置）

**顶点数（Vertex Count）**：
一局中顶点的个数，由玩家设定；硬顶为 200。
_Avoid_: 点数（口语可说，文档与代码用语用顶点数）、n（可作变量名，不作领域对外名）

**平面图保证（Planarity Guarantee）**：
每一局的抽象图都是平面图，因此一定存在无交叉画法；卡关是未找到布局，不是无解。
_Avoid_: 随机连边、可能无解模式（当前产品不做）

**圆上布局（Circle Layout）**：
自由模式与种子局的初始位置：均匀落在圆上；位置不进入种子。
_Avoid_: 主线关的初始布局（主线用图形排布，坐标进关卡文件）

**图形排布（Shape Layout）**：
关卡作者把点排成可辨认图形（可纠缠）；仅作开局嵌入。
_Avoid_: 通关剪影判定、必须像某图案才算赢

**交叉（Crossing）**：
两条边在非共享顶点处相交；全无交叉即通关。
_Avoid_: 碰撞、重叠（除非特指顶点重合）

### 复现与分享

**种子（Seed）**：
可复制、可粘贴的短码，用于复现同一局。v1 编码顶点数与连接关系（含生成用随机源），不含顶点坐标。
_Avoid_: 存档文件、关卡 ID（参照站用语，非本产品主名）

**随机一局（Reroll）**：
在自由模式当前顶点数下重新生成连接关系，并得到新种子。
_Avoid_: 下一关（战役用关卡表前进）

**进度（Progress）**：
存在本地的主线关号、皮肤入口解锁、自由模式解锁。
_Avoid_: 账号云存档（当前不做）

### 操作与视图

**拖点（Vertex Drag）**：
选中顶点并移动以改变嵌入，目标是消除交叉。
_Avoid_: 画线、编辑边

**相机（Camera）**：
视口的平移与缩放；画布可大于屏幕，使大顶点数下仍可点选与操作。
_Avoid_: 滚动页面（应用内手势，不是浏览器滚动画布的替代说法）

**重置视图（Reset View）**：
把相机调回使当前全部顶点落入视野的状态。
_Avoid_: 重启本局、重置布局

### 演进（有名、未做）

**主题 / 皮肤包（Theme / Skin Pack）**：
可替换的视觉与叙事包装；完成体中每个皮肤包带独立关卡。当前定妆一版；真皮肤包（人物点、选中音、特殊语音、气泡视频）在 C3。
_Avoid_: 皮肤商城式多主题切换（非当前里程碑）

**道具（Prop）**：
删边、删点、提示等消耗；接口预留，广告后接。首包主线不出现道具。
_Avoid_: 把看广告当成首包必做

**玩法变体（Variant）**：
限时、最少移动等规则加减；战役骨架稳定后再考虑。
_Avoid_: 模式（易与难度档混淆）

### 分发

**试玩链（Playtest Link）**：
Toy 上架前，将静态构建产物托管在 Pages 类服务上供他人游玩的链接。
_Avoid_: 正式发布、上架（专指 Toy 审核通过后）

**Toy**：
哔哩哔哩站内互动页托管；本产品的目标上架渠道，交付物为静态网页包。
_Avoid_: 小游戏开放平台（另一套 B 站能力）、App 商店
